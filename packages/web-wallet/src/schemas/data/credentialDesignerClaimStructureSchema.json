{
  "type": "object",
  "properties": {
    "credentialClaims": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "claimName": {
            "type": "string",
            "pattern": "^[A-Za-z_][A-Za-z0-9_-]*$"
          },
          "type": {
            "type": "string",
            "enum": ["object", "string", "number", "boolean", "array", "date"]
          },
          "required": {
            "type": "boolean"
          },
          "properties": {
            "type": "array",
            "items": {
              "$ref": "#/properties/credentialClaims/items"
            }
          }
        },
        "required": ["claimName", "type"]
      }
    }
  },
  "required": ["credentialClaims"]
}
